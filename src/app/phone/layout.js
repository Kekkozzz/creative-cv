export const metadata = {
  title: "Phone Assistant - Francesco Romito",
  description: "AI-powered Phone Assistant with real-time transcription",
};

export default function PhoneLayout({ children }) {
  return <>{children}</>;
}
